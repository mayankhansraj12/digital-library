# Digital Library Client (Frontend)

## Setup
1. `npm install`
2. Create `.env` file (see `.env.example`).
3. `npm run dev`

## Configuration
Update `.env` `VITE_API_URL` if backend is hosted remotely.

## Tech Stack
- React (Vite)
- Tailwind CSS
- Axios
- React Router DOM
